const introduce = (name, age, address) => {
    console.log (`Hi, my name is ${name}`);
    console.log (`I am ${age} years old`);
    console.log (`I live in ${address}`);
}
introduce (`Dhekoz`, 34, `Jakarta`);